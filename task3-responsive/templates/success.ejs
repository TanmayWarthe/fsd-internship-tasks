<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= title %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />
  <link rel="stylesheet" href="/styles.css" />
</head>
<body class="bg-light">
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4">
    <div class="container">
      <a class="navbar-brand" href="/">MyApp</a>
      <div class="collapse navbar-collapse">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a class="nav-link" href="/">Form</a></li>
          <li class="nav-item"><a class="nav-link" href="/submissions">Submissions</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <main class="container mb-5">
<div class="form-container">
<div class="card shadow p-4 animate__animated animate__zoomIn">
  <h2 class="mb-4 text-success text-center">Submission Successful 🎉</h2>
  <div class="text-center mb-3">
    <svg xmlns="http://www.w3.org/2000/svg" width="64" height="64" fill="currentColor" class="bi bi-check-circle-fill text-success" viewBox="0 0 16 16">
      <path d="M16 8A8 8 0 1 1 0 8a8 8 0 0 1 16 0m-3.97-3.03a.75.75 0 0 0-1.08.022L7.477 9.417 5.384 7.323a.75.75 0 0 0-1.06 1.06L6.97 11.03a.75.75 0 0 0 1.079-.02l3.992-4.99a.75.75 0 0 0-.01-1.05z"/>
    </svg>
  </div>
  <hr>
  <p><strong>Full Name:</strong> <%= data.fullname %></p>
  <p><strong>Email:</strong> <%= data.email %></p>
  <p><strong>Gender:</strong> <%= data.gender %></p>
  <p><strong>Hobbies:</strong> <%= data.hobbies.join(', ') || 'None' %></p>
  <p><strong>City:</strong> <%= data.city %></p>
  <p><strong>Agreed to Terms:</strong> <%= data.agreed ? 'Yes' : 'No' %></p>
  <p class="text-muted small"><strong>Submitted At:</strong> <%= new Date(data.submittedAt).toLocaleString() %></p>

  <div class="d-grid gap-2 mt-4">
    <a href="/" class="btn btn-primary">Submit Another Registration</a>
    <a href="/submissions" class="btn btn-outline-secondary">View All Submissions</a>
  </div>
</div>
</div>
  </main>

  <footer class="bg-dark text-white text-center py-3">
    <p class="mb-0">© <%= new Date().getFullYear() %> MyApp — All rights reserved.</p>
  </footer>
</body>
</html>
