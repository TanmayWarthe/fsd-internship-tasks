<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= title %></title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <main class="card">
    <h1>Contact Form</h1>

    <% if (Object.keys(errors).length) { %>
      <div class="flash">Please fix the errors below.</div>
    <% } %>

    <form action="/submit" method="POST">
      <label>Name
        <input name="name" value="<%= values.name || '' %>" required>
        <% if (errors.name) { %><small class="err"><%= errors.name %></small><% } %>
      </label>

      <label>Email
        <input type="email" name="email" value="<%= values.email || '' %>" required>
        <% if (errors.email) { %><small class="err"><%= errors.email %></small><% } %>
      </label>

      <label>Message
        <textarea name="message" rows="4" required><%= values.message || '' %></textarea>
        <% if (errors.message) { %><small class="err"><%= errors.message %></small><% } %>
      </label>

      <button type="submit">Send</button>
    </form>
  </main>
</body>
</html>
